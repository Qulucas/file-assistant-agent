# NOTES:文件助理 Agent 设计说明(半页)

## 循环与终止条件

单循环 `AgentLoop.run`:每轮 `llm.chat(messages, tools)` → 有 `tool_calls` 就依序执行、结果回填、继续;无工具调用即视为完成,输出最终答复。**循环内没有任何任务流程判断**,T1/T2 走同一个循环,下一步做什么完全由模型决定。

终止三态:(1) 模型输出纯文本 → 正常完成;(2) 步数上限(默认 30)→ 返回"已做 + 未完成 + 原因",不假装成功;(3) 安全熔断:连续 3 次相同 `(tool, args)` 注入"你在重复"提示,再犯(累计 6 次)强制终止。坏输出兜底:非法 JSON 参数 → 把解析错误作为工具结果回填让模型自纠;工具执行失败(文件不存在/越界)→ `Error: …` 文本给模型换方案;API 异常退避重试一次,再失败终止并说明。

## 上下文里塞什么、不塞什么

塞:system(安全声明 + 行为规则)、用户任务、工具 schema、每轮 assistant 消息与结构化工具结果(`<tool_result tool=… args=…>`)。不塞:超出截断线的文件原文、原始异常堆栈、被折叠的旧步骤细节。

三层预算:单次工具结果 ≤6000 字符;`read_file` 按行限量(默认 150 行,支持 offset 分页);消息总量超预算时从最早的工具往返开始折叠成一行摘要,永远保留 system 与最近步骤。974KB 的日志全程只经过 `search`(返回匹配行 + 行号),未整文件进上下文。

## 一个关键取舍:安全边界给代码,语义给模型

"不许动 active 草稿"、"最新文件为准"这类语义判断交给模型(它需要读内容、比较日期);但"路径不得越出 workspace"由 `WorkspaceSandbox.resolve()` 在代码层保证,与模型状态无关。代价:工具层无法阻止"模型合理范围内"的错误操作(如移错文件),只能事后靠 trace 审计;换来的是一旦模型被注入完全"说服",物理边界依然拒之门外——本次两处注入均未造成任何越权,校验脚本可证明。若换更严约束场景(如金融文件),可加白名单目录 + 写前双重确认,但会牺牲通用性,故未内置。

## 没做但知道该做的一件事

**结构化复盘/自我校验工具**:给模型一个 `verify_task()` 工具,在宣布完成前用独立路径(不读上下文里的历史结果)重新检查产物是否符合任务约束(比如统计归档文件数、核对索引行数)。当前靠模型自觉 + 外部 `verify_workspace.py`,后者只覆盖已知埋点;通用化后应把"自检"做进循环,防止模型在复杂任务上"自信地完成错事"。
